COLLECTION = {
    "basic": {
        "result_type": "text",
        "embedding_model": None,
        "index_size": "small"
    },
    "mini": {
        "result_type": "text",
        "embedding_model": None,
        "index_size": "small"
    }
}

TABLES = ['lcc', 'knbc', 'kwdlc']

MODEL = {
    "model_path": 'model/w2v_200d.model',
    "model_format": "full",
    "binary": True
}
